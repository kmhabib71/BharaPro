import { StyleSheet, Dimensions } from "react-native";
const styles = StyleSheet.create({
  displayCenter: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  addItem: {
    flexDirection: "row",
  },
});
